// Compile the source version of the index.ts
// Define the test configuration and become the file directly read by cucumber
// Does not support IE6 or Typescript. That's why we need to use Babel
module.exports = require('./dist')

